spring:
  config:
    on-profiles: production
  jpa:
    show-sql: true
    generate-ddl: true
    hibernate:
      ddl-auto: update
    database-platform: org.hibernate.dialect.PostgreSQLDialect
  datasource:
    url: jdbc:postgresql://localhost:5432/international-recipes
    username: dbUser
    password: password


server:
  port: 8082

